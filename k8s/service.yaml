#type: LoadBalancer
apiVersion: v1
kind: Service

metadata:
  name: pharmacy-service

spec:
  type: LoadBalancer

  selector:
    app: pharmacy-api

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080